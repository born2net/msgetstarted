#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../node-dev/bin/node-dev" "$@"
  ret=$?
else 
  node  "$basedir/../node-dev/bin/node-dev" "$@"
  ret=$?
fi
exit $ret
